using UnityEngine;

// dump.cs: public class BasicMenuClosed : StateMachineBehaviour (TypeDefIndex 7754)
// Owned by an AnimatorController state, not a prefab GameObject. The real class overrides
// OnStateEnter; zero serialized fields per the bundle type tree.
public class BasicMenuClosed : StateMachineBehaviour
{
}
