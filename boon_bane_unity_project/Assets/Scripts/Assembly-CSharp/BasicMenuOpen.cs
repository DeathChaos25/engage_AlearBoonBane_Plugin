using UnityEngine;

// dump.cs: public class BasicMenuOpen : StateMachineBehaviour (TypeDefIndex 7755)
// Owned by an AnimatorController state, not a prefab GameObject. The real class overrides
// OnStateEnter/OnStateExit; zero serialized fields per the bundle type tree.
public class BasicMenuOpen : StateMachineBehaviour
{
}
