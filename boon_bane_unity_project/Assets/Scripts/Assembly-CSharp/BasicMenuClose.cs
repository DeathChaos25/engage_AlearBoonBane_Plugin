using UnityEngine;

// dump.cs: public class BasicMenuClose : StateMachineBehaviour (TypeDefIndex 7753)
// Owned by an AnimatorController state (e.g. Fade.controller), not by any prefab GameObject.
// The real class overrides OnStateEnter/OnStateExit; we keep the body empty because the
// bundle's type tree shows zero serialized fields beyond the stock MonoBehaviour base.
public class BasicMenuClose : StateMachineBehaviour
{
}
